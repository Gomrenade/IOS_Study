var gemCounter = 0 

while true{
    moveForward()
    if isOnGem {
        collectGem()
        gemCounter = gemCounter + 1
    }
    if isBlocked {
        turnRight()
    }
    if isBlocked && isBlockedLeft {
        break
    }
}
